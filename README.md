Distance Caluclator App

Utilizing:
- SvelteKit (frontend)
- Express (api)
- PostgreSQL (db)
- Docker (build)

Start the application with:
`docker compose up --build`.

Navigate to http://localhost:5173/.
